name: 'dbt_ytmusic'
version: '1.0.0'
config-version: 2
profile: 'dbt_ytmusic'
model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_ytmusic:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
